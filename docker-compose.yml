services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      DATABASE_URL: sqlite+aiosqlite:////data/yorumla.db
      MCP_AUTH_TOKEN: change-me
      MAX_REVIEWS_PER_PRODUCT: "25"
      SCRAPER_TIMEOUT_MS: "12000"
      SCRAPER_TOTAL_TIMEOUT_SECONDS: "25"
      SCRAPER_MAX_PAGES: "2"
      BACKEND_CORS_ORIGINS: http://localhost:3000
    volumes:
      - yorumla-data:/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
    depends_on:
      - backend

volumes:
  yorumla-data:
